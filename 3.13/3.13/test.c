#include <stdio.h>
int main()
{

	int a = 0, y = 1;
	if (a == 0)
		if (y == 0)
			printf("正确\n");
	else
		{
			printf("错误\n");
		}
	case '\n':
		linecount++;
		//该处省略break语句
	case '\t':
	case ' ':
	return 0;
}
